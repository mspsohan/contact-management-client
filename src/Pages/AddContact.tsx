import { FC } from "react";
import NewContact from "../components/NewContact";

const AddContact: FC = () => {

   return (
      <div>
         <NewContact id={""} setOpen={() => { }} />
      </div>
   );
};

export default AddContact;
